<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.js.mapper.BoardMapper">

	<select id="getList" resultType="org.js.domain.BoardVO">
		<![CDATA[
			SELECT * FROM tbl_board WHERE bno > 0
		]]>
	</select>
	
	<insert id="insert">
		<![CDATA[
			INSERT INTO tbl_board (bno, title, content, writer)
			VALUES (seq_board.nextval, #{title}, #{content}, #{writer})
		]]>
	</insert>
	
	<insert id="insertSelectKey">
		<selectKey keyProperty="bno" order="BEFORE" resultType="Long">
			SELECT seq_board.nextval from dual
		</selectKey>
		<![CDATA[
			INSERT INTO tbl_board (bno, title, content, writer)
			VALUES (#{bno}, #{title}, #{content}, #{writer})
		]]>
	</insert>
	
	<select id="read" resultType="org.js.domain.BoardVO">
		<![CDATA[
			SELECT * FROM tbl_board WHERE bno = #{bno}
		]]>
	</select>
	
	<update id="update">
		<![CDATA[
			UPDATE tbl_board set title=#{title}, content=#{content}, writer=#{writer}, updateDate=sysdate WHERE bno=#{bno}
		]]>
	</update>
	
	<delete id="delete">
		<![CDATA[
			DELETE FROM tbl_board WHERE bno=#{bno}
		]]>
	</delete>
</mapper>